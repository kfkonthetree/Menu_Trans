# Menu_trans 项目详细说明

## 项目概述

Menu_trans 是一款专为餐厅菜单设计的智能翻译和饮食建议应用，主要面向需要在中文餐厅用餐的外国游客或有特殊饮食需求的用户。应用通过图像识别技术扫描菜单，结合AI翻译和饮食分析功能，为用户提供菜品的详细信息和个性化饮食建议。

## 主要功能模块 

### 1. 菜单扫描与识别
- **图像扫描**：支持通过相机拍摄或从相册选择菜单图片
- **文字识别**：使用AI技术识别图片中的菜品名称
- **多语言支持**：支持中英文菜品名称互译

### 2. 菜品信息获取
- **详细信息**：获取菜品的食材、烹饪方法、口味标签等
- **饮食分析**：根据用户的饮食偏好和限制，分析菜品是否适合食用
- **匹配评分**：为菜品与用户饮食偏好的匹配度提供评分

### 3. 个性化饮食建议
- **用户偏好设置**：支持设置饮食限制、口味偏好、素食/纯素选择等
- **安全提示**：对含有用户忌口食材的菜品进行提醒
- **推荐排序**：根据匹配度对菜品进行排序，推荐最适合用户的菜品

### 4. 历史记录与收藏
- **扫描历史**：保存用户的菜单扫描记录
- **点单记录**：记录用户的点单历史
- **收藏功能**：支持收藏喜欢的菜品到自定义收藏夹

### 5. 用户个人资料
- **基本信息**：用户个人资料管理
- **饮食偏好**：管理用户的饮食限制、口味偏好等

## 技术架构

### 前端
- **框架**：SwiftUI
- **语言**：Swift
- **主要组件**：
  - MainTabView：应用主标签视图
  - ContentView：扫描功能主界面
  - ResultsView：菜品结果展示界面
  - DishDetailView：菜品详情界面
  - HistoryView：历史记录界面
  - CollectionsView：收藏管理界面
  - ProfileView：个人资料界面

### 后端API
- **服务提供商**：SiliconFlow
- **API端点**：https://api.siliconflow.cn/v1/chat/completions
- **模型**：Pro/Qwen/Qwen2.5-VL-7B-Instruct
- **主要功能**：
  - 菜品识别
  - 菜品信息获取
  - 饮食分析

### 数据存储
- **本地存储**：UserDefaults
- **数据模型**：
  - DishInfo：菜品信息
  - UserProfile：用户个人资料
  - Order：订单信息
  - Collection：收藏夹

## 核心数据模型

### DishInfo
```swift
struct DishInfo: Codable, Identifiable, Hashable {
    var id = UUID()
    var chineseName: String     // 中文名
    var englishName: String     // 英文名
    var pinyinName: String?     // 拼音
    var ingredients: [String]   // 食材
    var cookingMethod: String   // 烹饪方法
    var flavorTags: [String]    // 口味标签
    var imageURL: URL?          // 图片URL
    var dietaryAnalysis: DietaryAnalysis? // 饮食分析
}
```

### DietaryAnalysis
```swift
struct DietaryAnalysis: Codable, Hashable {
    var isSafe: Bool            // 是否安全（适合食用）
    var conflictReason: String? // 不安全原因
    var preferenceMatch: Bool   // 是否符合偏好
    var matchReason: String?    // 符合偏好原因
    var matchScore: Int         // 匹配分数（0-100）
}
```

### UserProfile
```swift
struct UserProfile: Codable, Equatable {
    var restrictions: Set<String> = []      // 饮食限制
    var isVegetarian: Bool = false          // 是否素食
    var isVegan: Bool = false               // 是否纯素
    var flavorPreferences: Set<String> = [] // 口味偏好
    var spiceLevel: SpiceLevel = .none      // 辣度偏好
}
```

## API接口调用流程

### 1. 菜品识别
```
POST /v1/chat/completions
Headers: {
    "Authorization": "Bearer API_KEY",
    "Content-Type": "application/json"
}
Body: {
    "model": "Pro/Qwen/Qwen2.5-VL-7B-Instruct",
    "messages": [
        {
            "role": "system",
            "content": "You are an expert OCR engine specializing in Chinese restaurant menus..."
        },
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "请识别这张菜单中的所有菜品名称"},
                {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,..."}}
            ]
        }
    ]
}
```

### 2. 菜品信息获取
```
POST /v1/chat/completions
Headers: {
    "Authorization": "Bearer API_KEY",
    "Content-Type": "application/json"
}
Body: {
    "model": "Pro/Qwen/Qwen2.5-VL-7B-Instruct",
    "messages": [
        {
            "role": "system",
            "content": "You are an expert Chinese food critic and translator..."
        },
        {
            "role": "user",
            "content": "宫保鸡丁"
        }
    ]
}
```

## 应用流程

1. **用户设置**：用户在个人资料界面设置饮食偏好和限制
2. **菜单扫描**：用户拍摄或选择菜单图片
3. **菜品识别**：应用识别图片中的菜品名称
4. **信息获取**：为每个菜品获取详细信息和饮食分析
5. **结果展示**：根据匹配度排序展示菜品，提供安全提示
6. **点单与收藏**：用户可以选择菜品点单或收藏
7. **历史记录**：记录用户的扫描和点单历史

## 技术亮点

1. **AI驱动**：使用先进的AI技术进行菜单识别和菜品分析
2. **个性化推荐**：根据用户的饮食偏好提供个性化的菜品推荐
3. **实时分析**：实时分析菜品是否符合用户的饮食限制
4. **多语言支持**：支持中英文菜品名称和信息展示
5. **优雅的UI设计**：采用现代化的SwiftUI界面设计，提供良好的用户体验

## 项目结构

```
Menu_trans/
├── Assets.xcassets/              # 应用资源文件
├── en.lproj/                    # 英文本地化文件
├── CollectionDetailView.swift   # 收藏夹详情视图
├── CollectionPickerView.swift   # 收藏夹选择视图
├── CollectionsManager.swift     # 收藏管理
├── CollectionsView.swift        # 收藏视图
├── Config.swift                 # 配置文件
├── ContentView.swift            # 主界面
├── DishDetailView.swift         # 菜品详情视图
├── DishInfo.swift               # 菜品信息模型
├── DocumentScannerView.swift    # 文档扫描视图
├── HistoryCardView.swift        # 历史记录卡片视图
├── HistoryItem.swift            # 历史记录项模型
├── HistoryView.swift            # 历史记录视图
├── ImageService.swift           # 图片服务
├── Item.swift                   # 数据模型
├── LoadingView.swift            # 加载视图
├── MainTabView.swift            # 主标签视图
├── Menu_transApp.swift          # 应用入口
├── Order.swift                  # 订单模型
├── OrderHistoryDetailView.swift # 订单历史详情视图
├── OrderHistoryView.swift       # 订单历史视图
├── OrderListView.swift          # 订单列表视图
├── ProfileView.swift            # 个人资料视图
├── ProgressManager.swift        # 进度管理
├── ResultsView.swift            # 结果视图
├── ReviewSkippedView.swift      # 跳过菜品回顾视图
├── SiliconFlowAPIService.swift  # API服务
├── UserProfile.swift            # 用户个人资料模型
└── UserSettings.swift           # 用户设置
```

## 未来改进方向

1. **离线支持**：增加离线功能，支持在无网络环境下使用
2. **更多语言**：扩展支持更多语言的菜单识别和翻译
3. **增强AR功能**：增加AR菜单识别功能
4. **社区功能**：增加菜品评价和分享功能
5. **餐厅推荐**：根据用户的饮食偏好推荐适合的餐厅
6. **营养信息**：增加菜品的营养成分分析

## 总结

Menu_trans 是一款功能强大的智能菜单翻译和饮食建议应用，通过AI技术为用户提供个性化的饮食体验。应用结合了菜单扫描、菜品识别、饮食分析等功能，帮助用户在中文餐厅轻松点餐，享受美食。